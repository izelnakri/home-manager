require("izelnakri")

-- TODO: Auto-start RPC server, in future this might be needed for further automation, make it work for multi-session
-- vim.fn.serverstart('/tmp/nvim.sock')

-- NOTE: Maybe add <leader>g to g to config project, other pinned projects etc

-- The line beneath this is called `modeline`. See `:help modeline`
--/vim: ts=2 sts=2 sw=tt2 e
